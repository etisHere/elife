#include <stdlib.h> // Include the standard library for access to the system function.

void clear_screen() {
    system("clear"); // Executes the clear command in the terminal to clear the screen.
}

